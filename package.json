{
  "name": "quem-sou-eu",
  "version": "0.2.0",
  "private": true,
  "description": "Jogo de adivinhar quem você é, para jogar com os amigos na mesma mesa.",
  "scripts": {
    "build": "node build.js",
    "test": "node test-tudo.js",
    "dev": "node build.js && npx firebase emulators:start --project quem-sou-eu-e3e32",
    "deploy": "node build.js && npx firebase deploy --project quem-sou-eu-e3e32",
    "deploy:regras": "npx firebase deploy --only firestore:rules --project quem-sou-eu-e3e32",
    "deploy:site": "node build.js && npx firebase deploy --only hosting --project quem-sou-eu-e3e32",
    "test:regras": "firebase emulators:exec --only firestore --project demo-quem-sou-eu \"node test-regras.js\"",
    "sites": "npx firebase hosting:sites:list --project quem-sou-eu-e3e32",
    "entrar": "npx firebase login"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^15.29.0"
  }
}
